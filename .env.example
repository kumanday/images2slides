# =============================================================================
# Slides Infographic Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# All settings can also be overridden via CLI arguments.

# =============================================================================
# VLM Provider Configuration (required - choose one provider)
# =============================================================================

# Default VLM provider: google, openai, anthropic, or openrouter
VLM_PROVIDER=google

# Model to use (optional - defaults to provider's recommended model)
# Google default: gemini-3-pro-preview
# OpenAI default: gpt-5.2
# Anthropic default: claude-opus-4-5
# OpenRouter default: qwen/qwen3-vl-235b-a22b-instruct
# VLM_MODEL=gemini-3-pro-preview

# =============================================================================
# VLM API Keys (set the one matching your provider)
# =============================================================================

# Google AI API Key (for google provider)
# Get from: https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# OpenAI API Key (for openai provider)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Anthropic API Key (for anthropic provider)
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# OpenRouter API Key (for openrouter provider)
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# =============================================================================
# Google Slides API Authentication (required - choose one method)
# =============================================================================

# Option 1: OAuth 2.0 Client Secret (recommended for personal use)
# This will open a browser for authentication on first use.
# Download from: Google Cloud Console > APIs & Services > Credentials
CLIENT_SECRET_PATH=secrets/client_secret.json

# Option 2: Service Account (recommended for automation/servers)
# No browser interaction needed, but requires domain-wide delegation for
# accessing user files.
# Download from: Google Cloud Console > IAM & Admin > Service Accounts
# SERVICE_ACCOUNT_PATH=secrets/service_account.json

# =============================================================================
# Optional: Image Upload Configuration
# =============================================================================

# Google Cloud Storage bucket for uploading cropped image regions
# GCS_BUCKET=your-bucket-name

# =============================================================================
# SaaS Application Configuration (for docker-compose)
# =============================================================================

# Database URL for the SaaS backend
DATABASE_URL=postgresql+asyncpg://images2slides:dev_password@db:5432/images2slides

# Base URL for the web application (used for OAuth redirects)
APP_BASE_URL=http://localhost:3000

# Encryption key for storing OAuth tokens at rest (32+ chars)
TOKENS_ENCRYPTION_KEY=change_this_to_a_secure_random_string_32_chars

# Google OAuth credentials for user authentication
# Create at: Google Cloud Console > APIs & Services > Credentials > OAuth 2.0 Client IDs
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# NextAuth secret for session encryption
NEXTAUTH_SECRET=change_this_to_a_secure_random_string

# Storage configuration (local, s3, or gcs)
STORAGE_TYPE=local
LOCAL_STORAGE_PATH=/data/uploads
# S3_BUCKET=
# S3_REGION=us-east-1
# GCS_BUCKET=
